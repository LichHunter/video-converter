[project]
name = "video-converter"
version = "0.1.0"
description = "YouTube to MP3 converter"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "faster-whisper>=1.2.1",
    "pyyaml>=6.0.3",
    "yt-dlp>=2026.3.17",
]
